<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/albumItem_image_Image_View"
        android:layout_width="500dp"
        android:layout_height="300dp"
        android:layout_weight="7"
        android:scaleType="fitXY"
        android:layout_marginBottom="10dp"
        tools:src="@mipmap/piano"/>

    <TextView
        android:id="@+id/albumItem_song_Text_View"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/AlbumItem"
        tools:text="SONG NAME"
        />

    <TextView
        android:id="@+id/albumItem_album_Text_View"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/AlbumItem"
        tools:text="ALBUM NAME"/>

    <TextView
        android:id="@+id/albumItem_artist_Text_View"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/AlbumItem"
        tools:text="ARTIST NAME"/>

</LinearLayout>